# Family Bot

Telegram bot for family chats — stories, jokes, psychology advice, voice replies.
Built with FastAPI + Pydantic AI + YAML-spec agents.

## Quickstart

See `.env.example` for configuration.
Run `make install` then `make run`.
Full setup guide coming soon.
